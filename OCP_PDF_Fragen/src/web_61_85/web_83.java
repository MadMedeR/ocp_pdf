package web_61_85;

public class web_83 {
/*
 * Answer A  246   (values.stream method from Collections 
 which gives stream then
	<R> Stream<R> map(Function<? super T,? extends R> mapper) 
	Returns a stream consisting of the results of applying 
	the given function to the elements of this stream. 
 then
  .peek print  stream for every n 
 then we have count which gives long amount of elements(3) 
 but not saved anywhere and not printed 
 */
}
